# Dipendenze
node_modules

# Output di build TypeScript
dist
*.tsbuildinfo

# File generati da tsoa (routes Express + spec OpenAPI): si rigenerano con
# `npm run tsoa:gen`, non vanno committati né modificati a mano.
src/routes
build

*.log
.env

# Dump prodotti da backupService.ts (pg_dump): dati, non codice, e già
# tracciati come metadata nella tabella company_backups.
backups

# PDF di pre-fattura generati lato server: dati, non codice, e già tracciati
# come metadata nella tabella invoices (pdf_path, 0036).
invoices
